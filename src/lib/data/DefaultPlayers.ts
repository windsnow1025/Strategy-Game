import Player from "../Player";

const DefaultPlayers = [
  new Player(100, "Blue", "Blue Home"),
  new Player(100, "Red", "Red Home"),
  new Player(100, "Green", "Green Home")
];

export default DefaultPlayers;